name: Publish IG Posts

on:
  schedule:
    - cron: '45 18 * * *'   # 18:45 každý den
  workflow_dispatch:  # stále možnost ručního spuštění

jobs:
  wakeup-server:
    runs-on: ubuntu-latest

    steps:
      - name: Call Render Web Service
        run: |
          curl -X GET https://ig-publisher.onrender.com
